from new_graphene.typings import TypeSchema


class TypesPrinterMixin:
    """Utility class to print types in a readable format."""

    @staticmethod
    def print_schema(schema: TypeSchema):
        return 'something'
