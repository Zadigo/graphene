from new_graphene.fields.helpers import ImplicitField


class InputField(ImplicitField):
    """An `InputField` is used to define fields on `InputObjectType`. 
    It is similar to `graphene.Field` but with some differences.
    """
